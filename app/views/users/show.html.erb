<h1><%= @user.name %></h1>

<div>
  <%= @user.email %>
  <%= @user.password_digest %>

  <ul>
    <% @userblocks.each do |block| %>
      <li>
        <%= link_to block.name, block_path(block.id) %>
        <%= link_to "[edit]", edit_block_path(block.id) %>
        <%= link_to "[del]", block_path(block.id), method: :delete, data: { confirm: "are you sure?" } %>
      </li>
    <% end %>
  </ul>

</div>
